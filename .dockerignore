# =============================================================================
# .dockerignore - Excluir archivos del contexto de build de Docker
# =============================================================================
# Esto acelera el build y reduce el tamaño de la imagen.
# Los archivos aquí NO se copian al contenedor durante `docker build`.

# Version Control (1MB+)
.git/
.gitignore
.gitattributes

# Python cache y bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Virtual environments
.venv/
venv/
env/
.env

# Large Media & Models (mounted via volumes instead)
data/
models/
*.mp4
*.avi
*.mov
*.mkv
*.pth
*.pt
*.onnx
*.engine
*.vrs

# IDE & OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs & Temp
logs/
tmp/
*.log

# Documentation (not needed in container)
docs/
*.md
!README.md

# Docker files (no need to copy into image)
docker/
!docker/Video_Codec_SDK_13.0.37.zip
.dockerignore

# Test files
tests/
